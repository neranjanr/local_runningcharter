# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local sqlite
*.db
*.db-journal

# sheet secrets — gitignored local config (see config/sheet.example.json)
config/sheet.local.json
sheet.local.json
# mobile private build — single-file with hard-coded Script URL for phone (gitignored)
quicktrip-mobile/dist/Mobile_with_url_private.html

# uploaded samples / scratch — not needed to run project
.scratch/
*.html.bak
dashsample.html
sampletrend.html
AllTrips_sample.xlsx

.vercel
